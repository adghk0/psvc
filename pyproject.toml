[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "psvc"
version = "0.1.0"
description = "Lightweight Python-based service management framework"
authors = [
  { name="Eom Kyoungjun", email="adghk01234@gmail.com" }
]
readme = "README.md"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Homepage = "https://github.com/yourname/pyservice"
Documentation = "https://github.com/yourname/pyservice"